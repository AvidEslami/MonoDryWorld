# MonoDryWorld

```dotnet new install MonoGame.Templates.CSharp``` # Sets up vscode for MonoGame development, also download the MonoGame extension and the C# extension for best experience

```dotnet run``` # Runs the game
